# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# HTTP clients for external APIs
httpx>=0.27.0
aiohttp>=3.10.0

# Data handling (updated for Python 3.13 support)
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0  # For SQLite (development)
asyncpg>=0.29.0  # For PostgreSQL (production)

# Utilities
python-dateutil>=2.9.0
tenacity>=9.0.0

# Authentication (Firebase)
firebase-admin>=6.0.0

# Scheduling
APScheduler>=3.10.0

# Encryption
cryptography>=42.0.0
